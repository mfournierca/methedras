<h2>Listing Checklists</h2>

<table class="table">
  <thead>
    <tr>
      <th>Data</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for checklist <- @checklists do %>
    <tr>
      <td><%= checklist.data %></td>

      <td class="text-right">
        <span><%= link "Show", to: checklist_path(@conn, :show, checklist), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Edit", to: checklist_path(@conn, :edit, checklist), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Delete", to: checklist_path(@conn, :delete, checklist), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Checklist", to: checklist_path(@conn, :new) %></span>
